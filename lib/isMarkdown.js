// const markdownContent = 'Este es un ejemplo de enlace en Markdown.';
// const linkRegex = /\[([^\]]+)\]\(([^)]+)\)/g;
// let match;

// while ((match = linkRegex.exec(markdownContent)) !== null) {
//   const text = match[1];
//   const url = match[2];
//   console.log(`Encontrado un enlace: ${text} (${url})`);
// }
